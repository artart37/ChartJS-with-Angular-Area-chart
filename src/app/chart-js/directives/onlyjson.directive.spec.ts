import { OnlyJsonDirective } from './onlyjson.directive';

describe('OnlyjsonDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyJsonDirective();
    expect(directive).toBeTruthy();
  });
});
